# Archive

This directory contains older documents that no longer represent the primary source of truth.

They are kept for:

- decision history
- migration traceability
- older architectural context

Archive documents should not be used as current implementation references.
