# System References

These system documents are kept as architectural context and historical reference.

They may contain useful reasoning, but they are not the primary source of truth for the current implementation.
