{
  "name": "@ad-product-forge/api",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "start": "node dist/src/index.js"
  },
  "dependencies": {
    "@ad-product-forge/core": "0.1.0"
  },
  "devDependencies": {
    "typescript": "^5.6.2",
    "vitest": "^4.0.18"
  }
}