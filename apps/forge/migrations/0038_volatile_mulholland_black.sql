-- Snapshot synchronization only.
-- Migrations 0030 through 0036 were added without corresponding snapshots.
-- Their structural changes are already applied by those migrations, while
-- 0038_snapshot.json records the resulting canonical schema for future
-- drizzle-kit generations.

SELECT 1;
