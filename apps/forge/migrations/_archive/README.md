# Archived migrations

This directory contains migration files intentionally excluded from the active Drizzle migration path.

Archived SQL must never be copied back into the migrations root without validating its schema intent and the complete upgrade path. Structural schema changes belong in migrations generated by Drizzle Kit.
